POSTGRES_DB=postgraphile_template
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me

NODE_ENV=development
JWT_SECRET=change_me
DATABASE_URL=postgres://postgres:change_me@db:5432/postgraphile_template
NODE_TLS_REJECT_UNAUTHORIZED=0

# to init db when working locally with docker compose
DB_WORKDIR=/docker-entrypoint-initdb.d