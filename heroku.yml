setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    NODE_ENV: production
build:
  docker:
    db: db/Dockerfile
    web: api/Dockerfile
  config:
    DB_WORKDIR: db/up
release:
  image: db
  command:
    - echo "alo" && ls
run:
  web: npx node server/index.js