setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    NODE_ENV: production
build:
  docker:
    db: db/Dockerfile
    web: api/Dockerfile
  config:
    DB_WORKDIR: db/up
release:
  image: db
  command:
    - ls && ./db/up/up.sh
run:
  web: npx node server/index.js